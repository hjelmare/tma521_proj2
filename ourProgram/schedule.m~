clear all
clc


nSteps = 30;
nJobs = 15;
nMach = 5;

rest = dlmread('outRestricted.txt',' ',0,2);
pi = dlmread('outPi.txt',' ');
gamma = dlmread('outGamma.txt',' ');

%pi = reshape(pi,nJobs,nSteps)';
gamma = reshape(gamma, nMach, nSteps)

%plot(rest)
