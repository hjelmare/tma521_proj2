clear all
clc
clf


nJobs = 15;
nMach = 5;

sched = load('final_x_15jobs.txt');
%proc = load('asdf');

cc = jet(15);

for i = 1:nJobs
    plotParam = [sched(  ]
    rectangle('Position',[i,2,5,3],'FaceColor',cc(i,:))
end

axis([0,30,0,10])
